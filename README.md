
  # Web Portfolio Builder

  This is a code bundle for Web Portfolio Builder. The original project is available at https://www.figma.com/design/WguexrsdCpo2zgDWt8hApB/Web-Portfolio-Builder.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  